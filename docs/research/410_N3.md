# Neutral Three-Pulse Split

Date: `2026-05-24`

## What This Asks

Did the next `neutral` read clear the nine cool-edge seams inside
`20082..20096` when split into three smaller eval pulses of three rows each?

## Status

Yes. The split is now active evidence and the correction passed.

The prior `neutral` fail surface was coherent enough to justify a smaller
method read. The nine dominant cool-edge seams were not random spread. They
clustered into three visible pressure groups:

- lilac / mauve drift
- blue / jade drift
- mint / green drift

This note now records the smaller-pulse correction read. It does not replace
`Beta 1.0`; it narrows how the active `neutral` lane moved after the failed
third continuation.

## Current Surface

| Item | Current state |
| --- | --- |
| source fail surface | `20082..20096` |
| active proof surface | `20103..20105` |
| corrected split stack | `20097..20105` |
| active lane | `neutral` |
| source counted result | `4 anchors / 11 counted seams / 0 excluded` |
| corrected split result | `9 anchors / 0 counted seams / 0 excluded` |
| source fail shape | `9` cool-edge seams inside pale `neutral` |
| secondary residue | `2` warm seams |

## Completed Pulse Split

The next `neutral` read ran as three smaller eval pulses of three rows each:

| Pulse | Source ids | Corrected ids | Pressure group | Result |
| --- | --- | --- | --- | --- |
| `pulse 27` | `20082`, `20083`, `20094` | `20097..20099` | lilac / mauve | `3 anchors / 0 seams` |
| `pulse 28` | `20085`, `20090`, `20091` | `20100..20102` | blue / jade | `3 anchors / 0 seams` |
| `pulse 29` | `20086`, `20087`, `20095` | `20103..20105` | mint / green | `3 anchors / 0 seams` |

Corrected pulse rows:

- `pulse 27`: `Tapioca -> Gray sand`, `Creme brulee -> Tapioca`,
  `Petal pink -> Delicacy`
- `pulse 28`: `Sheer pink -> Sheer pink`, `Ecru -> Eggnog`,
  `Navajo -> Alabaster gleam`
- `pulse 29`: `Dew -> Powder puff`, `Powder puff -> Cream pink`,
  `Bridal blush -> Bridal blush`

Keep the two warm seams outside that split as secondary residue:

- `20084` `Parchment -> Novelle peach`
- `20088` `Pearled ivory -> Pearl`

## What Stays The Same

- `Beta 1.0` remains the active research method
- pulse verdict stays the live verdict unit
- `neutral` remains the active family lane
- the live DB keeps only the current proof surface

## Why This Helps

The source `15`-row fail surface answered the broad question honestly, but it
mixed three cool-edge problems together:

- lilac / mauve pressure
- blue / jade pressure
- mint / green pressure

Three smaller pulses made the correction easier to read:

- one correction can be judged against one pressure group at a time
- a surviving seam stays attributable instead of hiding inside a mixed tranche
- the method showed one undertone-bucket correction cleared the cool-edge split

## Operational Note

Current pulse tooling supports two smaller bounded pulse shapes:

- contiguous source-order runs through `huemiliator eval-pulse-start --count 3`
- thematic split runs through repeated `--input-hex` arguments

The exact `3 x 3` split used the explicit-input path. The command seeded each
thematic cool-edge group directly instead of pretending the groups were
contiguous source-order rows.

## What It Proves

The smaller split proves:

1. the lilac / mauve drift clears under neutral undertone-bucket selection
2. the blue / jade drift clears under the same correction
3. the mint / green drift clears under the same correction
4. the next question can move to a broader `neutral` continuation from source
   order `48`
