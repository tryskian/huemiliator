default_install_hook_types:
  - pre-commit
  - pre-push

repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-merge-conflict

  - repo: local
    hooks:
      - id: huemiliator-format-check
        name: huemiliator format check
        entry: make format-check
        language: system
        pass_filenames: false
        stages: [pre-commit, manual]

      - id: huemiliator-lint
        name: huemiliator lint
        entry: make lint
        language: system
        pass_filenames: false
        stages: [pre-commit, manual]

      - id: huemiliator-typecheck
        name: huemiliator typecheck
        entry: make typecheck
        language: system
        pass_filenames: false
        stages: [pre-push, manual]

      - id: huemiliator-test
        name: huemiliator test
        entry: make test
        language: system
        pass_filenames: false
        stages: [pre-push, manual]
